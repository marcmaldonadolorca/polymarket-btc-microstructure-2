# reports/

La memoria canónica es `memoria.pdf`. Su fuente está en `memoria/main.tex` y se
compila desde ese directorio con **tectonic**:

```text
tectonic main.tex
```

Las versiones breves anteriores se retiraron del árbol actual para evitar
confusión: el documento de entrega y referencia pública es únicamente
`memoria.pdf`.
